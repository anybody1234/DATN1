import { describe, it } from "vitest";

// Tests cho video flow và quiz flow nằm trong video-quiz-flows.test.tsx
describe("LessonPage", () => {
  it("tests covered in video-quiz-flows.test.tsx", () => {});
});
